spring:
  # Datasource (스키마는 Flyway V1이 생성)
  datasource:
    url: "jdbc:h2:mem:testdb;MODE=Oracle;DB_CLOSE_DELAY=-1"
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  # H2 Console
  h2:
    console:
      enabled: true
      path: /h2-console
