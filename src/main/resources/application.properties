spring.application.name=TripPhoto
server.forward-headers-strategy=framework

# GraphQL
spring.graphql.graphiql.enabled=true

# Actuator (Nginx가 /actuator/health만 통과시킴)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# Kakao OAuth2
spring.security.oauth2.client.registration.kakao.client-id=${KAKAO_CLIENT_ID:local-kakao-client-id}
spring.security.oauth2.client.registration.kakao.client-secret=${KAKAO_CLIENT_SECRET:local-kakao-client-secret}
spring.security.oauth2.client.registration.kakao.client-authentication-method=client_secret_post
spring.security.oauth2.client.registration.kakao.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.kakao.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.kakao.client-name=Kakao
spring.security.oauth2.client.provider.kakao.authorization-uri=https://kauth.kakao.com/oauth/authorize
spring.security.oauth2.client.provider.kakao.token-uri=https://kauth.kakao.com/oauth/token
spring.security.oauth2.client.provider.kakao.user-info-uri=https://kapi.kakao.com/v2/user/me
spring.security.oauth2.client.provider.kakao.user-name-attribute=id

# Internal authentication tokens. Override every secret outside local development.
auth.jwt.issuer=udumeoli
auth.jwt.secret=${JWT_SECRET:local-development-jwt-secret-change-before-deploying}
auth.jwt.access-token-ttl=30m
auth.jwt.refresh-token-ttl=14d
auth.jwt.signup-token-ttl=10m
auth.oauth-login-code-ttl=2m
auth.frontend-callback-url=${FRONTEND_LOGIN_CALLBACK_URL:http://localhost:3000/login/callback}
auth.cors-allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,https://udumeoli-web.vercel.app}
